<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= team.name %> - Team Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f0f0;
            font-family: 'Arial', sans-serif;
        }
        .navbar-brand {
            font-size: 1.5rem;
            font-weight: bold;
        }
        .team-header {
            background-color: #007bff;
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
        }
        .flag-frame {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            overflow: hidden;
            margin-right: 2rem;
            border: 5px solid white;
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
        }
        .flag-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .player-card {
            display: flex;
            align-items: center;
            background-color: white;
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 1rem;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .player-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 1rem;
        }
        .player-info {
            flex-grow: 1;
        }
        .btn-view-details {
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 20px;
            padding: 0.5rem 1rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">CricketMania</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/index"><i class="fas fa-home"></i> Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/players"><i class="fas fa-user"></i> Players</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/teams"><i class="fas fa-users"></i> Teams</a></li>
                    <li class="nav-item"><a class="nav-link" href="/matches"><i class="fas fa-trophy"></i> Matches</a></li>
                    <li class="nav-item"><a class="nav-link" href="/live_scores"><i class="fas fa-chart-line"></i> Live Scoring</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about"><i class="fas fa-info-circle"></i> About</a></li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <header class="team-header">
        <div class="container">
            <div class="d-flex align-items-center">
                <div class="flag-frame">
                    <img src="<%= team.image_path %>" alt="<%= team.name %> Flag">
                </div>
                <div>
                    <h1><%= team.name %></h1>
                    <p>Code: <%= team.code %> | Country ID: <%= team.country_id %></p>
                    <p>National Team: <%= team.national_team ? 'Yes' : 'No' %></p>
                    <p>Last Updated: <%= new Date(team.updated_at).toLocaleDateString() %></p>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        <h2 class="mb-4">Squad</h2>
        <div class="row">
            <div class="col-md-8 mx-auto">
                <% if (players && players.length > 0) { %>
                    <% const uniquePlayers = [...new Set(players.map(p => p.id))].map(id => players.find(p => p.id === id)); %>
                    <% uniquePlayers.forEach(player => { %>
                        <div class="player-card">
                            <img src="<%= player.image_path || '/default-player.png' %>" class="player-img" alt="<%= player.fullname %>">
                            <div class="player-info">
                                <h5 class="mb-1"><%= player.fullname %></h5>
                                <p class="mb-0">Position: <%= player.position ? player.position.name : 'N/A' %></p>
                            </div>
                            <a href="/player/<%= player.id %>" class="btn btn-view-details">View Details</a>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p class="alert alert-info text-center">No players found in the squad.</p>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>