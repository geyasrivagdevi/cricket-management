<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team Profile - <%= team.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-blue-200 to-blue-500 min-h-screen">
    <nav class="bg-blue-600 text-white p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold">CricketMania</a>
            <a href="/teams" class="hover:text-blue-200 transition duration-300">
                <i class="fas fa-arrow-left mr-2"></i>Back to Teams
            </a>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8">

        <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
            <div class="flex items-center mb-6">
                <img src="<%= team.image_path %>" alt="<%= team.name %>" class="w-32 h-32 rounded-full border-4 border-blue-500 shadow-lg mr-4">
                <div>
                    <h1 class="text-3xl font-bold text-gray-800"><%= team.name %></h1>
                    <p class="text-gray-600 text-lg">Code: <%= team.code %></p>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-gray-700">
                <div>
                    <div class="flex items-center mb-2">
                        <i class="fas fa-flag text-blue-500 mr-3"></i>
                        <span><strong>Country ID:</strong> <%= team.country_id %></span>
                    </div>
                    <div class="flex items-center mb-2">
                        <i class="fas fa-globe text-blue-500 mr-3"></i>
                        <span><strong>National Team:</strong> <%= team.national_team ? 'Yes' : 'No' %></span>
                    </div>
                </div>
                <div>
                    <div class="flex items-center mb-2">
                        <i class="fas fa-calendar-alt text-blue-500 mr-3"></i>
                        <span><strong>Updated At:</strong> <%= new Date(team.updated_at).toLocaleDateString() %></span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Squad Section -->
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Squad</h2>
        <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
            <% if (players && players.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                    <% players.forEach(player => { %>
                        <div class="bg-gray-100 rounded-lg p-4 flex items-center">
                            <img src="<%= player.image_path || '/default-player.png' %>" alt="<%= player.fullname %>" class="w-16 h-16 rounded-full mr-4">
                            <div>
                                <h3 class="font-semibold"><%= player.fullname %></h3>
                                <p class="text-sm text-gray-600"><%= player.position ? player.position.name : 'N/A' %></p>
                                <a href="/player/<%= player.id %>" class="text-blue-500 hover:underline text-sm">View Details</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="text-center text-gray-600">No players found in the squad.</p>
            <% } %>
        </div>

    </div>

    <!-- Footer -->
    <footer class="bg-blue-600 text-white text-center py=3 mt-auto">
        &copy; 2024 CricketMania. All rights reserved.
    </footer>

</body>
</html>
